class Solution:
    def hasDuplicate(self, nums: List[int]) -> bool:
        n=len(nums)
        for i in nums:
            count=0
            for a in range(0,n):
                if(nums[a]==i):
                    count+=1
            if count > 1:
                return True
  
        return False
